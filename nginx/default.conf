# サーバーブロック1: Hello Flaskアプリ用
# hello.mokko.jp という名前でアクセスが来た場合のルール
server {
    listen 80;
    server_name hello.mokko.jp;

    location / {
        # webappという名前のサービス(ポート5000)に転送する
        proxy_pass http://webapp:5000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}

# サーバーブロック2: テトリスゲーム用
# tetris.mokko.jp という名前でアクセスが来た場合のルール
server {
    listen 80;
    server_name tetris.mokko.jp;

    location / {
        # tetrisという名前のサービス(ポート80)に転送する
        proxy_pass http://tetris:80;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}